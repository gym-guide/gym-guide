package api.backend.dto;

public record ExerciseResponsePreviewDto(Long id,
                                         String title,
                                         String category,
                                         String imagePath) {
}
